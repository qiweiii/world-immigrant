---
import { defaultLocale, tf, type Locale } from "../i18n";

interface Props {
  title: string;
  country: string;
  category: string;
  summary: string;
  href?: string;
  status?: string;
  checkedAt?: string;
  locale?: Locale;
}

const {
  title,
  country,
  category,
  summary,
  href = "#",
  status = "active",
  checkedAt,
  locale = defaultLocale,
} = Astro.props;
---

<a class="card card-clickable" href={href}>
  <p class="card-meta">{country} · {category.replaceAll("_", " ")}</p>
  <h3 class="card-title">{title}</h3>
  <p class="card-description">{summary}</p>
  <div class="card-footer">
    {checkedAt && <span class="card-tag">{tf("card.updated", locale, { date: checkedAt.slice(0, 10) })}</span>}
  </div>
</a>